# Development artifacts
*.txt
!build/**/*.txt
# robots.txt is generated deployed output (build/scripts/common/build_sitemap.py) -- must be committed
!robots.txt
extract_images.js
image_urls.txt

# OS files
Thumbs.db
.DS_Store
# Python bytecode cache
__pycache__/
*.pyc

# Local Wrangler cache/metadata (from manual `wrangler pages deploy` runs) --
# not build output, not part of the deployed site, safe to ignore.
.wrangler/

# Local QA screenshots for owner review -- never committed or deployed.
qa-screenshots/

# Personal Claude Code overrides for this machine -- never committed.
.claude/settings.local.json
